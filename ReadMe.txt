Application parses Slack export JSON directories into an HTML report and CSV export.

SlackTool application is located @ dist/SlackTool/SlackTool.exe, but the provided shortcut can be moved to any location and used to run the application.

